<h1>Users</h1>

<p><%= link_to 'Add a New User', "/users/new" %></p>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= link_to 'Show', "/users/#{user.id}" %></td>
      <td><%= link_to 'Edit', "/users/#{user.id}/edit" %></td>
      <td><%= link_to 'Destroy', "/users/#{user.id}", method: 'delete', data: { confirm: "Do you really want to delete this user?" } %></td>

    </tr>
  <% end %>
  </body>
</table>

