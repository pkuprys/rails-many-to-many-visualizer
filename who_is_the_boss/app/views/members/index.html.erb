<h1>Listing members</h1>

<%= link_to 'New Member', new_member_path %>
</br>
<table>
  <tr>
    <th>Name</th>
    <th># of Hosts</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @members.each do |member| %>
  <tr>
    <td><%= member.name %></td>
    <td><%= member.hosts.count %></td>
    <td><%= link_to 'Show', member %></td>
    <td><%= link_to 'Edit', edit_member_path(member) %></td>
    <td><%= link_to 'Destroy', member, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

</br></br>
<h2>Graphs</h2>
<%=link_to 'Members By Host - Arc', graph_members_path(:hosts, :arc) %>
</br>
<%=link_to 'Members By Host - Matrix', graph_members_path(:hosts, :matrix) %>
